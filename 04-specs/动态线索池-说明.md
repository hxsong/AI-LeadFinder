# 动态线索池

> **最后更新**：2026-05-06
> **关联文档**：
>   - [模块设计](../02-modules/04-动态数据获客.md)
>   - [原型页面](../03-prototypes/pages/动态线索池.html)
>   - [功能说明-上游](../04-specs/访客雷达-说明.md)
>   - [功能说明-下游](../04-specs/动态线索详情-说明.md)

---

## 数据契约

| 前端展示字段 | 后端字段名 | 类型 | 来源/存储 | 说明 |
|-------------|-----------|------|----------|------|
| BOT ID | `bot_id` | `string` | 路由参数传入 | 当前 BOT 唯一标识 |
| 动态线索 ID | `dynamic_lead_id` | `string` | 系统生成 | 系统生成 UUID |
| 线索状态 | `status` | `string` | 系统状态机 | Resolving / Completed / ISP |
| IP 地址（脱敏） | `ip_address` | `string` | GeoIP 脱敏处理 | — |
| 地理位置 | `geo_location` | `string` | GeoIP 定位数据 | — |
| 设备信息 | `device_info` | `string` | 探针 JS 采集 UA 解析 | — |
| 流量来源 | `traffic_source` | `string` | 流量归因数据 | — |
| IP 解析进度 | `progress.ip_resolution` | `string` | IRS IP 解析服务 | pending / in_progress / completed / failed |
| 企业验证进度 | `progress.enterprise_verification` | `string` | 企业验证服务 | pending / in_progress / completed / failed |
| 联系人挖掘进度 | `progress.contact_mining` | `string` | Apollo.io 服务 | pending / in_progress / completed / failed |
| 企业名称 | `enterprise_info.name` | `string` | IRS IP 解析服务返回 | — |
| 企业域名 | `enterprise_info.domain` | `string` | IRS IP 解析服务返回 | — |
| 行业类型 | `enterprise_info.industry` | `string` | IRS IP 解析服务返回 | — |
| 联系人数量 | `contacts_count` | `number` | Apollo.io 联系人挖掘结果 | — |
| 联系人预览 | `contacts_preview` | `array` | Apollo.io | 前 1-3 个联系人简要信息 |
| Credits 消耗 | `credits_consumed` | `number` | Apollo.io Credits 实际消耗 | — |
| ISP 运营商名称 | `isp_name` | `string` | IRS IP 解析服务返回 | 宽带 ISP 名称 |
| 入库时间 | `created_at` | `string` | 系统生成 | ISO 8601 时间戳 |

---

## 页面流转

### 入口

| 来源 | 触发条件 | 携带参数 |
|------|---------|---------|
| 访客行为详情.html | 深度挖掘完成后自动跳转 | `bot_id`, `dynamic_lead_id` |
| 访客雷达.html | 点击进行中任务的进度卡片 | `bot_id`, `dynamic_lead_id` |
| 多BOT工作台.html | 点击侧边栏"动态线索池" | `bot_id` |

### 出口

| 目标 | 触发条件 | 携带数据 |
|------|---------|---------|
| 访客雷达.html | 点击"返回访客雷达"按钮 | `bot_id` |
| 动态线索详情.html | 点击已完成的线索卡片 | `bot_id`, `dynamic_lead_id` |

### 页面状态

| 状态 | 说明 | 进入条件 | 退出条件 |
|------|------|---------|---------|
| 加载中 | 页面加载/筛选变化 | 页面加载 | 数据返回 |
| 正常 | 数据返回成功 | 列表数据返回 | 用户操作 |
| 空态 | 无线索数据 | 列表为空 | 用户操作 |
| 进行中 | 存在 Resolving 任务 | 存在 Resolving 任务 | 任务完成 |
| ISP线索 | IP 解析为 ISP | IP 解析为 ISP | 用户操作 |
| 导出中 | 导出请求发出 | 点击导出按钮 | 导出完成 |

**状态流转图**：
```
加载中 --(数据返回)--> 正常 --(存在Resolving任务)--> 进行中 --(任务完成)--> 正常
加载中 --(数据为空)--> 空态
正常 --(IP解析为ISP)--> ISP线索
正常 --(点击导出)--> 导出中 --(导出完成)--> 正常
```

---

## 数据流

### 创建动态线索占位记录

```
POST /api/v1/dynamic-leads
```

**请求头**：`Authorization: Bearer {tenant_token}`
**Content-Type**：`application/json`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| visitor_log_id | `string` | 是 | 访客记录 ID |
| ip_address | `string` | 是 | IP 地址 |
| geo_location | `string` | 是 | 地理位置 |
| device_info | `string` | 是 | 设备信息 |
| traffic_source | `string` | 是 | 流量来源 |

**响应结构**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "dynamic_lead_id": "DYN-202602-001",
    "status": "Resolving",
    "created_at": "2024-02-27T10:05:00Z"
  }
}
```

**响应码说明**：

| code | 含义 |
|------|------|
| 200 | 创建成功 |
| 400 | 参数错误 |
| 404 | 访客记录不存在 |
| 500 | 服务异常 |

---

### 获取动态线索列表

```
GET /api/v1/dynamic-leads
```

**请求头**：`Authorization: Bearer {tenant_token}`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | `string` | 否 | 状态筛选（多选），逗号分隔：Resolving/Completed/ISP |
| sort | `string` | 否 | 排序方式：created_desc / created_asc |
| page | `number` | 否 | 页码，默认 1 |
| limit | `number` | 否 | 每页数量，默认 20 |

**响应结构**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 8,
    "items": [
      {
        "dynamic_lead_id": "DYN-202602-001",
        "status": "Resolving",
        "ip_address": "67.43.156.**",
        "geo_location": "Florida, USA",
        "device_info": "Desktop / Windows",
        "traffic_source": "Google 搜索",
        "progress": { "ip_resolution": "completed", "enterprise_verification": "completed", "contact_mining": "in_progress" },
        "created_at": "2024-02-27T10:05:00Z"
      },
      {
        "dynamic_lead_id": "DYN-202602-002",
        "status": "Completed",
        "enterprise_info": { "name": "Orlando Mega Stage AV", "domain": "orlando-av.com", "industry": "Events & AV Integration" },
        "contacts_count": 3,
        "contacts_preview": [{ "name": "John Doe", "title": "VP of Procurement", "is_primary": true }],
        "credits_consumed": 11,
        "created_at": "2024-02-27T09:30:00Z"
      },
      {
        "dynamic_lead_id": "DYN-202602-003",
        "status": "ISP",
        "ip_address": "67.43.156.**",
        "geo_location": "California, USA",
        "device_info": "Desktop / macOS",
        "traffic_source": "LinkedIn",
        "isp_name": "Comcast Cable Communications",
        "created_at": "2024-02-27T08:15:00Z"
      }
    ]
  }
}
```

**响应码说明**：

| code | 含义 |
|------|------|
| 200 | 查询成功 |
| 400 | 参数错误 |
| 401 | 未授权 |
| 500 | 服务异常 |

---

### 导出线索列表

```
GET /api/v1/leads/dynamic/export
```

**请求头**：`Authorization: Bearer {tenant_token}`

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | `string` | 否 | all / resolving / completed / isp |

**响应结构**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "download_url": "https://cdn.example.com/exports/dynamic_leads_20240120.xlsx",
    "expires_at": "2024-01-21T10:30:00Z"
  }
}
```

**响应码说明**：

| code | 含义 |
|------|------|
| 200 | 导出成功 |
| 400 | 参数错误 |
| 404 | 无数据可导出 |
| 500 | 服务异常 |

---

## 处理逻辑

### 场景一：查看进行中的挖掘任务

**触发条件**：系统检测到存在 Resolving 状态的线索挖掘任务。

**执行逻辑**：

1. 系统接收请求：页面加载，检测到 Resolving 任务
2. 系统执行处理：
   - 系统渲染进行中任务卡片，带脉冲动画和高亮边框
   - 卡片显示：IP 地址（脱敏）、访客基础信息（地区、设备、来源）、"挖掘解析中"状态标签
   - 进度动画展示：IP 企业解析状态 / 企业信息验证状态 / 联系人挖掘状态
   - 卡片边框高亮 + 背景渐变 + 脉冲动画
3. 系统返回响应：实时更新任务进度

**边界条件**：

| 条件 | 系统行为 |
|------|---------|
| 实时刷新 | 存在 Resolving 任务时，系统通过轮询或 WebSocket 推送实时更新任务进度 |
| 任务异步执行 | 前端页面关闭，挖掘任务在后台继续执行，不中断 |
| 接口超时 | 单次接口调用超时 1 分钟，跳过本次调用继续执行，不阻塞整体任务 |

**状态变更**：

| 原状态 | 新状态 | 触发条件 |
|--------|--------|----------|
| 加载中 | 进行中 | 存在 Resolving 任务 |
| 进行中 | 正常 | 任务完成 |

**输出**：进行中任务卡片，实时反映任务进度。

---

### 场景二：查看历史挖掘记录 - 成功线索

**触发条件**：系统检测到挖掘任务已完成（状态为 Completed）。

**执行逻辑**：

1. 系统接收请求：页面加载，检测到 Completed 任务
2. 系统执行处理：
   - 系统渲染已完成线索卡片：企业名称、企业域名（超链接）、所在地、行业类型
   - 展示卡片核心信息：联系人数量、Credits 消耗、入库时间
   - 点击卡片跳转至动态线索详情
3. 系统返回响应：页面渲染完成

**边界条件**：

| 条件 | 系统行为 |
|------|---------|
| 动态线索池 vs 静态线索池 | 两类数据独立管理，互不干扰 |

**状态变更**：无

**输出**：历史线索卡片列表，按入库时间倒序排列。

---

### 场景三：查看历史挖掘记录 - ISP 宽带线索

**触发条件**：系统检测到 IP 解析结果为公共宽带（ISP），无法精准定位企业。

**执行逻辑**：

1. 系统接收请求：页面加载，检测到 ISP 任务
2. 系统执行处理：
   - 企业名称显示"ISP 匿名访客"
   - 显示"ISP 宽带"警告标签和 ISP 运营商名称
   - 保留行为数据和地理位置信息
   - 联系人矩阵显示"未找到联系人"
3. 系统返回响应：页面渲染完成

**边界条件**：

| 条件 | 系统行为 |
|------|---------|
| 无 ISP 信息 | 显示"未知 ISP" |

**状态变更**：

| 原状态 | 新状态 | 触发条件 |
|--------|--------|----------|
| 加载中 | ISP线索 | IP 解析为 ISP |

**输出**：ISP 宽带线索卡片，特殊标识展示。

---

### 场景四：导出线索列表

**触发条件**：用户点击"导出线索列表"按钮。

**执行逻辑**：

1. 系统接收请求：用户点击导出按钮
2. 系统执行处理：
   - 系统发送 `GET /api/v1/leads/dynamic/export` 请求
   - 后端返回 Excel 下载链接（有效期 24 小时）
   - 浏览器自动下载或前端触发下载
3. 系统返回响应：返回下载链接

**边界条件**：

| 条件 | 系统行为 |
|------|---------|
| 筛选条件下无数据 | 返回 404，前端提示调整筛选条件 |

**状态变更**：

| 原状态 | 新状态 | 触发条件 |
|--------|--------|----------|
| 正常 | 导出中 | 用户点击导出按钮 |
| 导出中 | 正常 | 导出完成或失败 |

**输出**：线索列表 Excel 文件。

---

## 异常兜底

| 异常场景 | 检测方式 | 后端处理 | 前端表现 |
|---------|---------|---------|---------|
| IP 地址解析失败 | IP 解析服务返回失败 | 标记为 ISP 状态，记录失败原因 | ISP 线索卡片展示，Toast「IP 地址解析失败，无法识别企业信息」 |
| 企业信息验证未通过 | 验证服务返回失败 | 标记为失败状态，不进入 Completed | 失败提示，Toast「企业信息验证未通过，线索质量不达标」 |
| Credits 余额不足 | 联系人挖掘时余额不足 | 中断任务，记录欠费状态 | 任务中断，提示充值，Toast「Credits 余额不足，无法完成联系人挖掘」 |
| 挖掘任务意外中断 | 任务状态异常 | 自动重试机制，最多重试 3 次 | 自动重试尝试恢复，Toast「挖掘任务意外中断，正在尝试恢复」 |
| IP 无法解析到企业 | IP 解析服务无结果 | 标记为 ISP 状态 | ISP 线索展示，Toast「该 IP 无法解析到企业」 |
| 解析服务不可用 | IP 解析服务异常 | 任务暂停，进入重试队列 | 任务暂停，Toast「IP 解析服务暂时不可用，任务将自动重试」 |
| 网络请求超时 | 单次 API 调用超时 | 自动重试，记录超时日志 | 自动重试，Toast「网络请求超时，任务将自动重试，请稍候」 |
| 无数据可导出 | 筛选条件下无数据 | 返回 404 | 导出失败提示，Toast「当前筛选条件下无数据可导出，请调整筛选条件」 |
| 导出任务创建失败 | 后端异常 | 记录错误日志，返回 500 | 导出失败提示，Toast「导出任务创建失败，请稍后重试」 |
| 收藏列表达上限 | 收藏数超过系统限制 | 拒绝收藏请求，返回 400 | 收藏失败提示，Toast「收藏列表已达上限（{limit} 条），请清理后重试」 |
| 实时连接中断 | WebSocket 断开 | 尝试重连，降级为轮询 | 提示重连，Toast「实时连接已断开，正在尝试重新连接」 |
| 数据已过期 | 缓存过期 | 返回 304 或新数据 | 提示刷新，Toast「数据已过期，请刷新页面获取最新数据」 |
