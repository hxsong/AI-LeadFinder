# 多BOT工作台

> **版本**：v1.0.0
> **状态**：C（已确认）
> **对应 PRD 节点**：P03
> **对应原型页面**：多BOT工作台.html
> **最后更新时间**：2026-04-16
> **变更原因**：Phase 01 元数据增强，补充审核信息和模块归属
> **关联 Issue/PR**：-
> **模块归属**：01-账号与资源管理

---

## 概述

多BOT工作台是企业管理员（EBA）登录后的首个指挥中心，聚合展示全局资源（BOT 席位、Global Credits）和各获客引擎实例的运行状态，提供快速进入各业务模块的入口，并支持创建新的 BOT 实例。

---

## 页面入口与出口

### 入口

| 来源 | 触发条件 | 携带参数 |
|------|---------|---------|
| 登录页 | 登录成功后自动跳转 | - |

### 出口

| 目标 | 触发条件 | 携带数据 |
|------|---------|---------|
| 业务初始化.html | 点击 [+ 创建新 BOT]（无 BOT 时）或点击未初始化 BOT | `bot_id` |
| 业务初始化.html | 点击 [+ 创建新 BOT]（有 BOT 额度时） | `bot_id` |
| 购买更多算力模态框 | 点击 [购买更多算力] | - |
| 访客雷达.html | 点击侧边栏"访客雷达" | `bot_id` |
| 静态线索池.html | 点击侧边栏"静态线索池" | `bot_id` |
| 动态线索池.html | 点击侧边栏"动态线索池" | `bot_id` |
| 提示词语言管理.html | 点击侧边栏"提示词语言管理" | `bot_id` |

---

## 功能逻辑

### 场景一：查看全局资源看板

**触发条件**：管理员进入工作台。

**执行逻辑**：
1. 页面加载时调用工作台数据接口
2. 展示全局 Credits 余额（全账号名共享资金池）
3. 展示 BOT 席位使用情况（已创建 / 总额度）
4. 展示 BOT 实例列表，每个卡片显示：名称、状态、累计线索数、Credits 消耗

**输出**：完整工作台 dashboard。

---

### 场景二：创建新 BOT 实例

**触发条件**：管理员点击 [+ 创建新 BOT] 占位卡片。

**执行逻辑**：
1. 前端检测 BOT 席位是否充足（bot_seats_available > 0）
2. 若额度充足，弹出创建表单，输入 BOT 名称
3. 提交后调用创建接口，返回 bot_id
4. 前端轮询 bot 初始化状态（GET /api/v1/bot/{bot_id}/init-status，间隔 2 秒）
5. 初始化成功（status = initialized）后跳转业务初始化页面

**输出**：新 BOT 创建成功，跳转至业务初始化。

---

### 场景三：进入已有 BOT 工作空间

**触发条件**：管理员点击已有 BOT 卡片的 [进入获客引擎]。

**执行逻辑**：
1. 前端将 current_active_bot_id 设置为选中的 bot_id
2. 此后该用户所有操作被严格锁定在该 BOT 的数据作用域内
3. 不同 BOT 的配置互不交叉污染

**输出**：进入目标 BOT 的工作空间，侧边栏模块均使用该 BOT 的数据。

---

### 场景四：BOT 状态自动同步

**触发条件**：BOT 对应的动态数据探针运行状态变化。

**执行逻辑**：
1. 状态判断以动态数据获客（JS 探针）运行状态为准
2. 最近 500 次上报中错误率恢复至 ≤5%，自动重置为"正常运行"
3. 24 小时内恢复数据上报，自动重置为"正常运行"
4. 无需人工干预，状态自动同步

**输出**：BOT 卡片状态标签实时反映探针健康度。

---

### 场景五：购买更多算力

**触发条件**：管理员点击"购买更多算力"按钮。

**执行逻辑**：
1. 弹出【购买更多算力】模态框
2. 提示"请联系销售或客服人员"，显示官网联系方式
3. 不直接跳转支付，引导线下联系

**输出**：展示联系引导模态框。

---

## 页面状态

| 状态 | 触发条件 | UI 表现 |
|-----|---------|---------|
| 正常 | 数据完整返回 | 展示工作台 dashboard |
| 加载中 | 请求发出后 | 骨架屏或 loading 动画 |
| 创建中 | BOT 创建请求发出 | 创建表单 loading |
| 初始化中 | 轮询初始化状态 | 进度提示 + 禁止操作 |
| 额度不足 | bot_seats_available = 0 | 创建卡片置灰禁用 |

---

## 数据流

### 请求一：获取工作台数据

```
GET /api/v1/tenant/workspace
```

**请求头**：`Authorization: Bearer {tenant_token}`

**响应体**：

```json
{
  "code": 200,
  "data": {
    "global_credits_balance": 35000,
    "bot_seats_used": 1,
    "bot_seats_total": 3,
    "bot_seats_available": 2,
    "bots": [
      {
        "bot_id": "BOT-001",
        "bot_name": "北美区-户外屏主站",
        "status": "running",
        "static_leads_count": 48,
        "dynamic_leads_count": 12,
        "credits_consumed": 850,
        "created_at": "2024-01-10T09:00:00Z"
      }
    ]
  }
}
```

### 请求二：创建 BOT

```
POST /api/v1/bot/create
```

**请求体**：

```json
{
  "bot_name": "欧洲区-LED显示屏"
}
```

### 请求三：轮询 BOT 初始化状态

```
GET /api/v1/bot/{bot_id}/init-status
```

**响应体**：

```json
{
  "code": 200,
  "data": {
    "bot_id": "BOT-002",
    "status": "initialized",
    "message": "BOT 初始化完成"
  }
}
```

---

## 边界规则

| 规则 | 条件 | 系统行为 |
|------|------|---------|
| 数据隔离 | 点击进入任意 BOT | 全局 current_active_bot_id 锁定该 BOT，后续所有操作在该 BOT 数据域内 |
| BOT 额度限制 | bot_seats_available = 0 | [+ 创建新 BOT] 卡片置灰禁用，提示联系销售 |
| 初始化轮询 | 创建 BOT 后 | 轮询间隔 2 秒，终止条件：status = initialized（成功）或 failed（失败） |
| 探针错误恢复 | 500 次上报错误率降至 ≤5% | BOT 状态自动恢复为"正常运行" |
| 探针断线恢复 | 24 小时内恢复上报 | BOT 状态自动恢复为"正常运行" |
| Credits 余额 | 余额为 0 | 侧边栏提示充值，搜客和挖掘功能受限 |

---

## API 清单

| 接口 | 方法 | 说明 | 调用时机 |
|------|------|------|---------|
| /api/v1/tenant/workspace | GET | 获取工作台数据 | 页面加载 |
| /api/v1/bot/create | POST | 创建 BOT 实例 | 点击创建按钮 |
| /api/v1/bot/{bot_id}/init-status | GET | 轮询初始化状态 | 创建后轮询 |

---

## 异常处理

| 异常场景 | 检测方式 | 前端表现 | Toast 文案 |
|---------|---------|---------|-----------|
| BOT 创建失败 | code 非 200 | 失败提示 | 「BOT 创建失败，请稍后重试」 |
| BOT 初始化失败 | status = failed | 失败提示 + 重试按钮 | 「BOT 初始化失败，请重试」 |
| 初始化超时 | 轮询超时（2 分钟） | 超时提示 | 「BOT 初始化超时，请稍后重试」 |
| 无 BOT 额度 | bot_seats_available = 0 | 创建卡片禁用 | 「BOT 席位已用完，请联系销售扩充」 |
| 网络超时 | 请求超时 | 错误提示 | 「网络连接异常，请检查网络后重试」 |
| 服务不可用 | code 500 | 全局错误处理 | 「服务暂时不可用，请稍后再试」 |

---

## 字段映射

| 前端展示字段 | 后端字段名 | 数据来源说明 |
|-------------|-----------|------------|
| Global Credits 余额 | global_credits_balance | 全账号名共享资金池余额 |
| BOT 席位（已用/总计） | bot_seats_used / bot_seats_total | 当前 BOT 实例数 / 总额度 |
| 可用 BOT 席位 | bot_seats_available | 剩余可创建数量 |
| 当前活跃 BOT | current_active_bot_id | 前端状态管理 |
| BOT ID | bot_id | 系统生成 |
| BOT 名称 | bot_name | 用户创建时定义 |
| BOT 状态 | status | running / initializing / failed |
| 静态线索数 | static_leads_count | 静态线索池汇总 |
| 动态线索数 | dynamic_leads_count | 动态线索池汇总 |
| Credits 消耗 | credits_consumed | 该 BOT 累计消耗 |
| 创建时间 | created_at | ISO 8601 时间戳 |
