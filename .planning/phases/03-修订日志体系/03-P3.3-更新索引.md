---
phase: 03
plan: P3.3
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
canonical_refs:
  - docs/README.md
  - docs/changelog/v1.0.0.md
  - docs/changelog/v1.1.0.md
  - .planning/phases/03-修订日志体系/03-CONTEXT.md
requirements: []
autonomous: true
---

<objective>
更新 `docs/README.md`，将 changelog/ 目录新增的 v1.0.0.md（历史变更回填）和 v1.1.0.md（本次变更记录）追加到设计文档总索引中，并完善 changelog/ 目录说明段落。
</objective>

<context>
## D-04（已有内容保留）
- docs/README.md 索引更新仅追加 changelog/v1.1.0.md 条目
- 不修改现有其他索引条目
- 不修改 HTML 设计文档本身内容

## D-03（Markdown vs HTML 关系）
- docs/README.md 第 36 行「HTML与Markdown同步更新」声明在本次执行后得以兑现

## v1.1.0.md 记录内容（用于索引描述）
- Phase 1：功能说明文档重写（21份）
- Phase 2：双向索引映射表建立
- Phase 3：修订日志体系建立
- Phase 3：文档索引更新（docs/README.md）
</context>

<tasks>

<task>
<id>T1</id>
<type>execute</type>
<read_first>
- docs/README.md
- docs/changelog/v1.0.0.md
- docs/changelog/v1.1.0.md
</read_first>
<action>
更新 `docs/README.md`，分两处修改：

**修改 1：目录结构图（文件约第 10-20 行）**

将现有的：
```markdown
docs/
├── README.md                    # 本文件，设计文档总索引
├── design/                      # 产品设计文档（可视化）
│   ├── LeadFinder修订日志.html
│   ├── LeadFinder业务流设计.html
│   └── LeadFinder实体关系图.html
├── changelog/                   # 修订日志（Markdown，可用于 Git diff）
│   └── v1.0.0.md
└── 产品原型结构分析报告.md       # 文档结构优化分析报告
```

更新为：
```markdown
docs/
├── README.md                    # 本文件，设计文档总索引
├── design/                      # 产品设计文档（可视化）
│   ├── LeadFinder修订日志.html
│   ├── LeadFinder业务流设计.html
│   └── LeadFinder实体关系图.html
├── changelog/                   # 修订日志（Markdown，可用于 Git diff）
│   ├── v1.0.0.md              # 回填 v1.0.0~v1.1.16 完整历史变更记录
│   └── v1.1.0.md              # 本次文档体系重构变更记录（Phase 1~3 成果）
└── 产品原型结构分析报告.md       # 文档结构优化分析报告
```

**修改 2：changelog/ 目录说明段落（文件约第 34-36 行）**

将现有的：
```markdown
存放 Markdown 格式的修订日志，用于 Git 版本管理和变更追溯。HTML 版本（design/）与 Markdown 版本（changelog/）内容同步更新。
```

更新为：
```markdown
存放 Markdown 格式的修订日志，用于 Git 版本管理和变更追溯。HTML 版本（design/）与 Markdown 版本（changelog/）内容同步更新。

| 版本 | 日期 | 状态 | 说明 |
|------|------|------|------|
| v1.0.0 | 2026-04-03 | C（已确认） | 初始版本，含产品原型初版发布记录（v1.0.0~v1.1.16 历史变更回填） |
| v1.1.0 | 2026-04-16 | C（已确认） | 本次文档体系重构变更记录：功能说明文档重写（21份）+ 双向索引映射表建立 + 修订日志体系建设 |
```

**保留不动的内容：**
- 目录结构图中的 design/ 部分
- 目录说明中的 design/ 说明段落
- 关联文档引用段落
- 文件顶部元数据块
</action>
<acceptance_criteria>
- docs/README.md 目录结构图中 changelog/ 下新增 v1.0.0.md 和 v1.1.0.md 两行（含注释说明）
- changelog/ 说明段落中新增版本对照表（版本/日期/状态/说明四列）
- 原有 design/ 目录说明段落内容不变
- 顶部元数据块不变
- HTML与Markdown同步声明（第36行附近）不被删除
</acceptance_criteria>
<verify>
# 验证目录结构图包含两个 changelog 条目
grep -q "v1\.0\.0\.md.*回填" docs/README.md && echo "CHANGELOG_ENTRY1=OK"
grep -q "v1\.1\.0\.md.*本次文档体系重构" docs/README.md && echo "CHANGELOG_ENTRY2=OK"
# 验证版本对照表存在
grep -q "v1\.0\.0.*2026-04-03.*初始版本" docs/README.md && echo "TABLE_ROW1=OK"
grep -q "v1\.1\.0.*2026-04-16.*文档体系重构" docs/README.md && echo "TABLE_ROW2=OK"
# 验证原有内容未被修改
grep -q "HTML与Markdown同步" docs/README.md && echo "SYNC_NOTE=OK"
</verify>
</task>

</tasks>

<verification>
1. 确认 docs/README.md 目录结构图包含 v1.0.0.md 和 v1.1.0.md（含注释）
2. 确认 changelog/ 说明段落中包含版本对照表
3. 确认 HTML与Markdown同步注释未被删除
4. 确认 design/ 原有内容未被改动
5. 确认 docs/changelog/v1.0.0.md 和 v1.1.0.md 均已创建（P3.1 和 P3.2 先完成）
</verification>

<success_criteria>
- docs/README.md 索引正确引用 changelog/v1.0.0.md 和 changelog/v1.1.0.md
- changelog/ 说明段落清晰说明两个版本的内容范围
- 原有索引内容完全保留不变
- docs/README.md 第 36 行「HTML与Markdown同步更新」声明得以兑现
</success_criteria>
